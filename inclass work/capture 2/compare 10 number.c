#include<stdio.h>
int main()
{
    int max,n,a[10];
    for(n=0;n<=9;n++)
    {
       scanf("%d",&a[n]);
    }
    max=0;
    for(n=0;n<=9;n++)
    {
       if (max>=a[n])
       void();
       else
         max=a[n];
    }
    printf("%d",max);
    return 0;
}
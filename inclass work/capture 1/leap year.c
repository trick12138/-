#include<stdio.h>
int main()
{
    int a,b,i;
    scanf("%d,%d",&a,&b);
    for ( i = a; i < b; i++)
    {
        if (i%4==0)
          if(i%100!=0)
          printf("%d\n",i);
          else
            if(i%400==0)
            printf("%d\n",i);
            else
            void();
        else
        void();
    }
    
    return 0;
}